var M=[[],[],[]], s1=0, s2=0, s3=0;
for (c=0; c<3; c++) {
	for (i=0; i<3; i++) {
		M[c][i]=Number(prompt());
	}
}
for (i=0; i<3; i++) {
	s1+=M[0][i];
	s2+=M[1][i];
	s3+=M[2][i];
}
alert(s1+"\n"+s2+"\n"+s3);
